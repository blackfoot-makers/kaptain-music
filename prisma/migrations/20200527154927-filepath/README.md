# Migration `20200527154927-filepath`

This migration has been generated by GreeFine at 5/27/2020, 3:49:27 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Music" (
"author" TEXT NOT NULL  ,"filename" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,"image" TEXT NOT NULL  ,"name" TEXT NOT NULL  ,"path" TEXT NOT NULL  ,FOREIGN KEY ("id") REFERENCES "Tag"("id") ON DELETE CASCADE ON UPDATE CASCADE)

INSERT INTO "quaint"."new_Music" ("author", "id", "image", "name", "path") SELECT "author", "id", "image", "name", "path" FROM "quaint"."Music"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Music";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Music" RENAME TO "Music";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200525143416-image_requiered..20200527154927-filepath
--- datamodel.dml
+++ datamodel.dml
@@ -1,11 +1,12 @@
 datasource sqlite {
   provider = "sqlite"
-  url = "***"
+  url      = "file:./catalog.db"
 }
 generator client {
-  provider = "prisma-client-js"
+  provider      = "prisma-client-js"
+  binaryTargets = ["windows", "native"]
 }
 model Tag {
   id    Int     @default(autoincrement()) @id
@@ -14,11 +15,12 @@
   Music Music[]
 }
 model Music {
-  id     Int    @default(autoincrement()) @id
-  name   String
-  author String
-  image  String
-  path   String
-  tags   Tag    @relation(fields: [id], references: [id])
-}
+  id       Int    @default(autoincrement()) @id
+  name     String
+  author   String
+  image    String
+  path     String
+  filename String
+  tags     Tag    @relation(fields: [id], references: [id])
+}
```


